# Phase-3 additions on top of Phase-2's requirements.txt
# (mcp, bcrypt, pydantic, rank_bm25, scikit-learn, hnswlib, numpy, langchain-core, etc.
#  — see ../Phase-2/requirements.txt, reused as-is)

langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
anthropic>=0.40.0

# CV file upload -> raw text extraction (state_graph/faculty_hiring/cv_text_extraction.py)
pypdf>=4.0.0
python-docx>=1.1.0

# Platform (admin + user surface)
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
